# Default config for sway
#
# Copy this to ~/.config/sway/config and edit it to your liking.
#
# Read `man 5 sway` for a complete reference.

### Variables
#
# Logo key. Use Mod1 for Alt.
set $mod Mod4
# Home row direction keys, like vim
set $left h
set $down j
set $up k
set $right l
# My preferred terminal emulator
set $term kitty
# My preferred application launcher
set $menu wofi --show drun
# My preferred file manager
set $files thunar
# My preferred browser
set $browser brave-nightly

# --- Environment Variables for Portals ---
# Force apps to understand we are in Sway/Wayland
exec dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP=sway

# Initialize GNOME Keyring
# This ensures the daemon is running with support for secrets (passwords) and SSH keys
exec /usr/bin/gnome-keyring-daemon --start --components=pkcs11,secrets,ssh

# --- Monitor Variables ---
set $mon1 DP-2
set $mon2 HDMI-A-1

# --- FONT (Added based on request) ---
font pango:FantasqueSansM Nerd Font 11

### Output configuration
#
# Default wallpaper (more resolutions are available in /usr/share/backgrounds/sway/)
output * bg /home/tomast1337/Pictures/mono_wallpaper.png fill
#
# Example configuration:
#
#   output HDMI-A-1 resolution 1920x1080 position 1920,0
#
# You can get the names of your outputs by running: swaymsg -t get_outputs

# Theming
client.focused          #ffffff  #000000  #ffffff  #ffffff  #ffffff
client.focused_inactive #333333  #000000  #888888  #000000  #333333
client.unfocused        #333333  #000000  #888888  #000000  #333333
client.urgent           #bf616a  #bf616a  #ffffff  #bf616a  #bf616a
client.placeholder      #000000  #000000  #ffffff  #000000  #000000

client.background       #000000

# --- Window Decoration ---
default_border pixel 2          # No text by default, just a border
default_floating_border pixel 2
gaps inner 5
gaps outer 0

# Enable text bars
default_border normal 1
default_floating_border normal 1

# Shrink the font specifically for titlebars (keep your system font normal)
font pango:FantasqueSansM Nerd Font 8
titlebar_border_thickness 1
titlebar_padding 1

# Remove extra fat/padding
titlebar_border_thickness 1
titlebar_padding 1

### Idle configuration
#
# Example configuration:
#
exec swayidle -w \
    timeout 600 'swaymsg "output * power off"' resume 'swaymsg "output * power on"' \
    before-sleep 'swaylock -f -c 000000'
#
# This will lock your screen after 300 seconds of inactivity, then turn off
# your displays after another 300 seconds, and turn your screens back on when
# resumed. It will also lock your screen before your computer goes to sleep.

### Input configuration
#
# Example configuration:
#
#   input type:touchpad {
#       dwt enabled
#       tap enabled
#       natural_scroll enabled
#       middle_emulation enabled
#   }
#
input type:keyboard {
    xkb_layout br,lt,us
    xkb_options grp:win_space_toggle
}
#
# You can also configure each device individually.
# Read `man 5 sway-input` for more information about this section.

### Key bindings
#
# Basics:
#
    # Start a terminal
    bindsym $mod+Return exec $term

    # Kill focused window
    bindsym $mod+Shift+q kill

    # Start your launcher
    bindsym $mod+d exec $menu

    # Open file manager
    bindsym $mod+Shift+e exec $files

    # Open browser
    bindsym $mod+Shift+b exec $browser

    # Drag floating windows by holding down $mod and left mouse button.
    # Resize them with right mouse button + $mod.
    # Despite the name, also works for non-floating windows.
    # Change normal to inverse to use left mouse button for resizing and right
    # mouse button for dragging.
    floating_modifier $mod normal

    # Clipboard manager
    bindsym $mod+p exec cliphist list | wofi --dmenu | cliphist decode | wl-copy

    # Reload the configuration file
    bindsym $mod+Shift+c reload

    # Exit sway (logs you out of your Wayland session)
    bindsym $mod+Alt+Shift+e exec swaynag -t warning -m 'You pressed the exit shortcut. Do you really want to exit sway? This will end your Wayland session.' -B 'Yes, exit sway' 'swaymsg exit'

    # Lock screen and suspend
    # Power Menu (Wofi)
    bindsym $mod+Shift+z exec ~/.config/sway/power_menu.sh
#
# Moving around:
#
    # Move your focus around
    bindsym $mod+$left focus left
    bindsym $mod+$down focus down
    bindsym $mod+$up focus up
    bindsym $mod+$right focus right
    # Or use $mod+[up|down|left|right]
    bindsym $mod+Left focus left
    bindsym $mod+Down focus down
    bindsym $mod+Up focus up
    bindsym $mod+Right focus right

    # Move the focused window with the same, but add Shift
    bindsym $mod+Shift+$left move left
    bindsym $mod+Shift+$down move down
    bindsym $mod+Shift+$up move up
    bindsym $mod+Shift+$right move right
    # Ditto, with arrow keys
    bindsym $mod+Shift+Left move left
    bindsym $mod+Shift+Down move down
    bindsym $mod+Shift+Up move up
    bindsym $mod+Shift+Right move right
#
# Workspaces:
#
    # Switch to workspace
    #bindsym $mod+1 workspace number 1
    #bindsym $mod+2 workspace number 2
    #bindsym $mod+3 workspace number 3
    #bindsym $mod+4 workspace number 4
    #bindsym $mod+5 workspace number 5
    #bindsym $mod+6 workspace number 6
    #bindsym $mod+7 workspace number 7
    #bindsym $mod+8 workspace number 8
    #bindsym $mod+9 workspace number 9
    #bindsym $mod+0 workspace number 10
    # Move focused container to workspace
    #bindsym $mod+Shift+1 move container to workspace number 1
    #bindsym $mod+Shift+2 move container to workspace number 2
    #bindsym $mod+Shift+3 move container to workspace number 3
    #bindsym $mod+Shift+4 move container to workspace number 4
    #bindsym $mod+Shift+5 move container to workspace number 5
    #bindsym $mod+Shift+6 move container to workspace number 6
    #bindsym $mod+Shift+7 move container to workspace number 7
    #bindsym $mod+Shift+8 move container to workspace number 8
    #bindsym $mod+Shift+9 move container to workspace number 9
    #bindsym $mod+Shift+0 move container to workspace number 10
    # Note: workspaces can have any name you want, not just numbers.
    # We just use 1-10 as the default.
   # --- Workspace Assignments (Lock workspaces to monitors) ---
    # Adjust 'DP-2' and 'HDMI-A-1' if your monitor names change.
    # Primary Monitor (Left/Center) gets 1-9
    workspace 1 output $mon1
    workspace 2 output $mon1
    workspace 3 output $mon1
    workspace 4 output $mon1
    workspace 5 output $mon1
    workspace 6 output $mon1
    workspace 7 output $mon1
    workspace 8 output $mon1
    workspace 9 output $mon1
    workspace 0 output $mon1

    # Secondary Monitor (Right) gets 11-15
    workspace 11 output $mon2
    workspace 12 output $mon2
    workspace 13 output $mon2
    workspace 14 output $mon2
    workspace 15 output $mon2
    workspace 16 output $mon2
    workspace 17 output $mon2
    workspace 18 output $mon2
    workspace 19 output $mon2
    workspace 20 output $mon2

    # --- Synced Switching (The "Scene" Logic) ---
    # Pressing Mod+Number switches BOTH monitors at once.

    bindsym $mod+1 exec "swaymsg workspace 1; swaymsg workspace 11; swaymsg focus output DP-2"
    bindsym $mod+2 exec "swaymsg workspace 2; swaymsg workspace 12; swaymsg focus output DP-2"
    bindsym $mod+3 exec "swaymsg workspace 3; swaymsg workspace 13; swaymsg focus output DP-2"
    bindsym $mod+4 exec "swaymsg workspace 4; swaymsg workspace 14; swaymsg focus output DP-2"
    bindsym $mod+5 exec "swaymsg workspace 5; swaymsg workspace 15; swaymsg focus output DP-2"
    bindsym $mod+6 exec "swaymsg workspace 6; swaymsg workspace 16; swaymsg focus output DP-2"
    bindsym $mod+7 exec "swaymsg workspace 7; swaymsg workspace 17; swaymsg focus output DP-2"
    bindsym $mod+8 exec "swaymsg workspace 8; swaymsg workspace 18; swaymsg focus output DP-2"
    bindsym $mod+9 exec "swaymsg workspace 9; swaymsg workspace 19; swaymsg focus output DP-2"
    bindsym $mod+0 exec "swaymsg workspace 0; swaymsg workspace 20; swaymsg focus output DP-2"

    # --- Move Container to "Scene" ---
    # Moving a window to '1' puts it on the main screen, '11' puts it on the side screen.
    # We keep the shift binds simple:

    bindsym $mod+Shift+1 exec ~/.config/sway/smart_move.py 1
    bindsym $mod+Shift+2 exec ~/.config/sway/smart_move.py 2
    bindsym $mod+Shift+3 exec ~/.config/sway/smart_move.py 3
    bindsym $mod+Shift+4 exec ~/.config/sway/smart_move.py 4
    bindsym $mod+Shift+5 exec ~/.config/sway/smart_move.py 5
    bindsym $mod+Shift+6 exec ~/.config/sway/smart_move.py 6
    bindsym $mod+Shift+7 exec ~/.config/sway/smart_move.py 7
    bindsym $mod+Shift+8 exec ~/.config/sway/smart_move.py 8
    bindsym $mod+Shift+9 exec ~/.config/sway/smart_move.py 9
    bindsym $mod+Shift+0 exec ~/.config/sway/smart_move.py 0
    
    # Throw current window to the other monitor (Keep focus on it)
    bindsym $mod+Shift+m move container to output right; focus output right
#
# Layout stuff:
#
    # You can "split" the current object of your focus with
    # $mod+b or $mod+v, for horizontal and vertical splits
    # respectively.
    bindsym $mod+b splith
    bindsym $mod+v splitv

    # Switch the current container between different layout styles
    bindsym $mod+s layout stacking
    bindsym $mod+w layout tabbed
    bindsym $mod+e layout toggle split

    # Make the current focus fullscreen
    bindsym $mod+f fullscreen

    # Toggle the current focus between tiling and floating mode
    bindsym $mod+Shift+space floating toggle

    # Swap focus between the tiling area and the floating area
    bindsym $mod+space focus mode_toggle

    # Move focus to the parent container
    bindsym $mod+a focus parent
#
# Scratchpad:
#
    # Sway has a "scratchpad", which is a bag of holding for windows.
    # You can send windows there and get them back later.

    # Move the currently focused window to the scratchpad
    bindsym $mod+Shift+minus move scratchpad

    # Show the next scratchpad window or hide the focused scratchpad window.
    # If there are multiple scratchpad windows, this command cycles through them.
    bindsym $mod+minus scratchpad show
#
# Resizing containers:
#
mode "resize" {
    # left will shrink the containers width
    # right will grow the containers width
    # up will shrink the containers height
    # down will grow the containers height
    bindsym $left resize shrink width 10px
    bindsym $down resize grow height 10px
    bindsym $up resize shrink height 10px
    bindsym $right resize grow width 10px

    # Ditto, with arrow keys
    bindsym Left resize shrink width 10px
    bindsym Down resize grow height 10px
    bindsym Up resize shrink height 10px
    bindsym Right resize grow width 10px

    # Return to default mode
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+r mode "resize"
#
# Utilities:
#
    # Special keys to adjust volume via PulseAudio
    bindsym --locked XF86AudioMute exec pactl set-sink-mute \@DEFAULT_SINK@ toggle
    bindsym --locked XF86AudioLowerVolume exec pactl set-sink-volume \@DEFAULT_SINK@ -5%
    bindsym --locked XF86AudioRaiseVolume exec pactl set-sink-volume \@DEFAULT_SINK@ +5%
    bindsym --locked XF86AudioMicMute exec pactl set-source-mute \@DEFAULT_SOURCE@ toggle
    # Special keys to adjust brightness via brightnessctl
    bindsym --locked XF86MonBrightnessDown exec brightnessctl set 5%-
    bindsym --locked XF86MonBrightnessUp exec brightnessctl set 5%+
    # Special key to take a screenshot with grim
    #bindsym Print exec grim

#
# Status Bar:
#
# Read `man 5 sway-bar` for more information about this section.
# bar {
#     position top
# 
#     # When the status_command prints a new line to stdout, swaybar updates.
#     # The default just shows the current date and time.
#     status_command while date +'%Y-%m-%d %X'; do sleep 1; done
# 
#     colors {
#         statusline #ffffff
#         background #323232
#         inactive_workspace #32323200 #32323200 #5c5c5c
#     }
# }
# --- Status Bar (WAYBAR) ---
# We replaced the default bar block with this to use Waybar
bar {
    swaybar_command waybar
}

# --- Clipboard Manager ---
# Use exec_always to ensure it runs
exec wl-paste --type text --watch cliphist store 
exec wl-paste --type image --watch cliphist store

# custom window rules
# move pavucontrol to the right side of the 32 px below the top of the screen
for_window [app_id="pavucontrol"] floating enable, resize set 1280 720, move position 640 0
for_window [class="Spotify"] floating enable, resize set 1280 720, move position center
# Prevent Spotify from stealing focus when track changes
for_window [class="Spotify"] urgent disable
no_focus [class="Spotify"]
# Steam Friends (Runs on XWayland, use 'title' or 'class')
for_window [title="Friends List"] floating enable, resize set 600 800, move position 640 0
# Brave Picture in Picture
for_window [title="^Picture in picture$"] floating enable, resize set 640 360, move position center, sticky enable
# MPV player
for_window [app_id="mpv"] floating enable, resize set 640 360, move position center, sticky enable

# --- Thunar (File Manager) ---
# Floating rename dialogs and progress bars (copy/move)
for_window [app_id="thunar" title="^Rename"] floating enable, resize set 600 200, move position center
for_window [app_id="thunar" title="File Operation Progress"] floating enable, resize set 600 200, move position center
for_window [app_id="thunar" title="Confirm to replace files"] floating enable, resize set 600 200, move position center

# --- qBittorrent ---
# Main app tiles, but "Add Torrent", "Options", and "Properties" float
for_window [app_id="org.qbittorrent.qBittorrent" title="^Add New Torrent"] floating enable, resize set 800 600, move position center
for_window [app_id="org.qbittorrent.qBittorrent" title="^Options"] floating enable, resize set 800 600, move position center
for_window [app_id="org.qbittorrent.qBittorrent" title="^Retrieve Torrent"] floating enable, resize set 600 200, move position center

# --- Godot 4 (XWayland Mode) ---

# 1. The Running Game (Debug Window)
# This finds any window with class "Godot" where the title ends in "(DEBUG)"
for_window [class="Godot" title=".*\(DEBUG\)$"] floating enable, move position center

# 2. Editor Dialogs (Settings, Export, etc.)
for_window [class="Godot" title="^Project Settings"] floating enable, move position center
for_window [class="Godot" title="^Editor Settings"] floating enable, move position center
for_window [class="Godot" title="^Export"] floating enable, move position center
for_window [class="Godot" title="^Create New"] floating enable, move position center

# 3. Small popups/tooltips (Color pickers, etc)
for_window [class="Godot" window_role="pop-up"] floating enable

# --- pqiv (Image Viewer) ---
# Force it to float, set a reasonable size, and center it
for_window [app_id="pqiv"] floating enable, resize set 900 600, move position center
for_window [class="pqiv"] floating enable, resize set 900 600, move position center

# --- General System Dialogs ---
# "Open File", "Save As", "Print" dialogs in almost any app
for_window [window_role="pop-up"] floating enable
for_window [window_role="bubble"] floating enable
for_window [window_role="dialog"] floating enable
for_window [window_type="dialog"] floating enable

# --- Custom Monitor Scripts ---
# F1 = Work Mode (Dual Screen)
bindsym $mod+F1 exec ~/.config/sway/monitor_manager.py work

# F2 = Movie Mode (Mirroring)
bindsym $mod+F2 exec ~/.config/sway/monitor_manager.py movie

# Toggle border
bindsym $mod+Shift+t border toggle

# --- Media Player Controls ---
# Play/Pause (F7)
bindsym --locked XF86AudioPlay exec playerctl play-pause
# Next Track (F8)
bindsym --locked XF86AudioNext exec playerctl next
# Previous Track (F6)
bindsym --locked XF86AudioPrev exec playerctl previous
# Stop (F5)
bindsym --locked XF86AudioStop exec playerctl stop

# --- Screenshots & Recording ---
# Pressing Print opens the Capture Menu
# If recording is active, pressing it again stops the recording.
bindsym Print exec ~/.config/sway/capture.sh

# Optional: Keep the instant shortcuts if you want them too
bindsym Control+Print exec grim -g "$(slurp)" - | wl-copy

# Assign applications to workspaces
assign [class="Spotify"] workspace 10
assign [class="Slack"] workspace 9
# specific whatpulse window usually doesn't have a class until opened, 
# but it starts minimized to tray usually.
assign [class="WhatPulse"] workspace 8


# --- Autostart ---

# Bluetooth
exec blueman-applet

# Update Environment Variables (CRITICAL)
# This tells systemd and D-Bus where the keyring is, so apps launched later can find it.
#exec_always dbus-update-activation-environment --systemd DISPLAY WAYLAND_DISPLAY SWAYSOCK SSH_AUTH_SOCK

# Monitor Setup
# We use 'exec' so it runs on login. 
exec_always ~/.config/monitor/work_mode.sh

# Authentication Polkit (Required for GUI apps needing sudo)
exec /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1

# System Tray & Notifications
exec swaync
exec nm-applet --indicator

# Applications
# WhatPulse
exec /usr/bin/whatpulse --delaystartup

# Spotify
# I removed '--type=zygote' so the UI actually opens, but kept your sandbox/cache flags.
exec /opt/spotify/spotify --no-sandbox --change-stack-guard-on-fork=enable --user-data-dir=/home/tomast1337/.cache/spotify --log-severity=disable

# Slack
# Standard launch (Wayland automatically handles the zygote processes)
# Slack with Keyring support
exec /usr/bin/slack --password-store=gnome --enable-features=UseOzonePlatform --ozone-platform=wayland

# Auto-tiling
exec_always autotiling

# include /etc/sway/config.d/*
